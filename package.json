{
  "name": "nestplan",
  "private": true,
  "type": "module",
  "scripts": {
    "test:rules": "npx --yes firebase-tools@14.17.0 emulators:exec --only firestore --project nestplan-rules-test \"node --test tests/firestore-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.1",
    "firebase": "^12.14.0"
  }
}
